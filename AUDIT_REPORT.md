# SPEC-1 Audit Report

**Date:** 2026-05-20  
**Branch audited:** `claude/audit-spec-1-repo-YBbIi`  
**Auditor:** Claude Code (claude-sonnet-4-6)  
**Baseline test count (CLAUDE.md):** 736

---

## 1. TL;DR

**What's broken:** `pip install -e ".[dev]"` fails on the standard pip path (sgmllib3k build error); `spec1_api/main.py` has duplicate imports; `memory/context.md` and `CLAUDE.md` are materially stale (still reference the removed `cls_quant` module, wrong test count, wrong MCP tool count, missing three new modules).

**What's risky:** Commit `b377099` added four prompt files directly to `src/spec1_engine/core/prompts/` — the frozen core — without any recorded human approval, in violation of governance rules. Commit `713429d` ("Potential fix for pull request finding") silently bootstrapped the entire codebase (185 files, 149K insertions) under an opaque message. Three gitignore-listed artifact types (`briefs/`, `spec1_intelligence_export.json`, `spec1_political_web.html`) are tracked in git. `tweepy` is used in the X publisher but not declared in `pyproject.toml`.

**What's fine:** Test suite is healthy — 1009 passing, 2 skipped (expected: no weasyprint), 0 failures. No secrets committed. No TODO/FIXME/HACK. No hardcoded paths. All expected modules exist and imports resolve cleanly.

---

## 2. Commit Findings

### Git state
- Branch: `claude/audit-spec-1-repo-YBbIi` — clean working tree, no stash, no detached HEAD
- Diverged from `main` by ~107 files (doc additions, REPAIR_PLAN, FALLBACK_SETUP, ROADMAP)
- No merge conflicts, no force-push artifacts detected in current history window

### Commit table (last 50)

| Hash | Date | Message | Issue | Severity |
|------|------|---------|-------|----------|
| `713429d` | 2026-05-01 | "Potential fix for pull request finding" | **185 files, 149,996 insertions** — silently bootstraps entire codebase. Message bears no relationship to diff. | HIGH |
| `fc9a477` | 2026-05-19 | "docs: clarify repo strategy in ROADMAP.md (#75)" | Labeled `docs` but removes 9 `cls_quant` Python modules + modifies `mcp_server.py`, `cls_db/models.py`, `spec1_api/dependencies.py`, `spec1_engine/app/cycle.py`. 35 files, 2859 deletions hidden under docs label. | HIGH |
| `b377099` | 2026-05-15 | "feat(briefing): add geopolitics and legislative desk brief modes (v0.5.0)" | Adds 4 new `.md` files directly to `src/spec1_engine/core/prompts/` — the **frozen core**. CLAUDE.md states any change to `core/` requires explicit human approval. No approval is documented. | HIGH |
| `742141a` | 2026-05-19 | "Phase 1: Consolidation – quant removal, API versioning..." | Mega-commit: 29 files, 2008 insertions. Touches quant removal, API versioning (/api/v1/), verdict UI, dual-write, docs, schemas, and Makefile simultaneously. Impossible to revert surgically. | MEDIUM |
| `d91d298` + `b259d89` | 2026-05-19 | Two commits both labeled "fix" for briefing writer fallback, same day | Same root cause (briefing fallback) patched twice in sequence — suggests first fix was incomplete and deployed, indicating the fix wasn't tested end-to-end. | MEDIUM |
| `2494b44` | 2026-05-10 | "Merge branch 'main' into feedback-loop" | 108 files, 137,203 insertions — a merge-into-feature-branch containing virtually the whole codebase. Normal for the first merge from main, but the scale warrants a note. | LOW |
| `5cb6453` | 2026-05-06 | "restore: bring back Apr 11-18 briefs..." | Deliberately commits brief artifacts to `briefs/` — a gitignored directory — with a stated rationale ("no-delete-data policy"). The gitignore intent and the commit intent are in direct conflict; the directory remains tracked. | LOW |
| `3b1fff0` | 2026-05-19 | "Initial plan (#81)" | Zero-diff commit — Copilot agent planning artifact with no file changes. | LOW |
| `b06c557` | 2026-04-20 | "Update schemas.py" | Vague message, 1-line change (version string). Harmless but undetectable from the log. | LOW |
| `0cfa876` `14568dc` `22a119a` `eb7cced` `b29bcd8` `6b82a92` | 2026-05-10 | "Potential fix for pull request finding" (×6) | These six are small doc tweaks (1–3 lines each). The message is auto-generated by Copilot Autofix and conveys nothing about what was changed. | LOW |
| `639bb69` | 2026-05-19 | "Potential fix for code scanning alert no. 12..." | Adds `permissions` block to a workflow. Message is adequately descriptive; the "Potential fix" prefix is Copilot boilerplate but not misleading here. | INFO |

### Commits that touch tests without code (or vice versa)

- `79d9518` — only removes 92 lines from `tests/test_verdicts.py` / `test_calibration.py` (fixes duplicate class shadowing). No code change. Acceptable: test-only fix.
- `d432b15` ("three-tier LLM fallback client") — adds 1148 lines across 9 files including `tests/test_fallback_client.py`. Code and tests updated together. ✅
- `fc523ed` ("Legislative & Judicial Desk") — 15 files, 2283 insertions, includes `tests/test_leg_jud.py`. ✅

---

## 3. Structural Integrity

### Expected modules (per CLAUDE.md)

| Module | Dir exists | `__init__.py` | Imports resolve | Tests | Status |
|--------|-----------|---------------|-----------------|-------|--------|
| `cls_osint` | ✅ | ✅ | ✅ | `test_feed.py`, `test_fara.py`, `test_narrative.py`, `test_pipeline.py` | **OK** |
| `cls_world_brief` | ✅ | ✅ | ✅ | `test_world_brief.py` | **OK** |
| `cls_leads` | ✅ | ✅ | ✅ | `test_leads.py` | **OK** |
| `cls_psyop` | ✅ | ✅ | ✅ | `test_psyop.py`, `test_psyop_evidence.py` | **OK** |
| `cls_quant` | ❌ REMOVED | — | — | `test_quant.py` REMOVED | **REMOVED (intentional)** |
| `cls_db` | ✅ | ✅ | ✅ | `test_persistence.py` | **OK** |
| `spec1_api` | ✅ | ✅ | ✅ | `test_api.py` | **OK** |
| `mcp_server.py` | ✅ | n/a | ✅ | `test_mcp_server.py` | **OK** |

### Modules present but NOT documented in CLAUDE.md

| Module | Status | Notes |
|--------|--------|-------|
| `cls_leg_jud` | Fully implemented | Legislative & Judicial Desk — added in `fc523ed`. Has schemas, producer, formatter, store, tests. |
| `spec1_engine/llm/` | Fully implemented | Three-tier fallback client (Claude → Ollama → rule-based). Added in `d432b15`. Has tests. |
| `spec1_engine/psyop/` | Partially overlapping | Internal psyop scorer used by `app/cycle.py`. Different implementation from `cls_psyop`. Two parallel psyop implementations exist with no documented reconciliation plan. |
| `spec1_engine/schemas/brief.py` | Implemented | `WorldStateBrief`, `BriefSection`, `SectionKind` dataclasses. |
| `spec1_api/db/signals.py` | Implemented | API-level SQLite signal insert/query for political signals ingest router. |
| `spec1_api/routers/ingest.py` | Implemented | `POST /api/v1/ingest/signal` — n8n crawler intake; env-gated behind `SPEC1_POLITICAL_WEB_ENABLED`. |
| `spec1_api/routers/nodes.py` | Implemented | Node registry for political web; same env gate. |
| `spec1_api/routers/publication.py` | Implemented | `POST /api/v1/publication/generate` — on-demand publication PDF. |
| `spec1_api/routers/workspace.py` | Implemented | `/api/v1/workspace` case management routes. |
| `spec1_engine/app/publishers/x.py` | Implemented | X/Twitter thread publisher. |
| `cls_db/publish_log.py` | Implemented | Append-only publish log JSONL for X publisher idempotency. |

### CLAUDE.md vs reality gaps

1. **`cls_quant` still referenced in CLAUDE.md 4 times** — module was removed in `fc9a477` / `2d369b5`. CLAUDE.md governance section still lists it in agent write surfaces.
2. **`test_quant.py` referenced in CLAUDE.md** — file deleted; the workaround note is stale.
3. **Internal re-export shims** (`spec1_engine/cls_leads/`, `spec1_engine/cls_psyop/`, `spec1_engine/cls_world_brief/`) mentioned in CLAUDE.md — these directories do not exist. The re-exports were never implemented as described.
4. **MCP tool count** — CLAUDE.md lists 12 tools; `mcp_server.py` exposes 15 (includes `run_psyop`, `generate_brief`, `generate_leads` added later).
5. **Test baseline** — CLAUDE.md says "27 files, ~780 tests passing"; actual count is 37 test files, 1011 collected.
6. **`memory/context.md`** — still shows `v0.4.0`, lists `cls_quant` as active, shows 12 MCP tools, references `.[quant]` install extras. Significantly stale.

### Code quality issues

- **`spec1_api/main.py` duplicate imports** — `from fastapi import FastAPI` on lines 10 and 12; `from spec1_api.routers import publication` on lines 29 and 31. Harmless at runtime (Python deduplicates), but indicates copy-paste noise from incremental edits.
- **Two parallel psyop implementations** — `spec1_engine/psyop/scorer.py` (used by `app/cycle.py`) and `cls_psyop/` (used by MCP tools, operator CLI). They implement different interfaces and classification schemes. No documented plan to unify.
- **`tweepy` undeclared dependency** — `spec1_engine/app/publishers/x.py` does `import tweepy` guarded by `try/except ImportError`, but `tweepy` does not appear in `pyproject.toml`. Publishing to X will silently fall back if tweepy is absent; no warning is logged.

---

## 4. Test Suite

### Summary

| Metric | Value |
|--------|-------|
| CLAUDE.md baseline | 736 |
| README baseline | ~780 |
| Collected (this run) | 1011 |
| Passed | 1009 |
| Skipped | 2 |
| Failed | 0 |
| Delta from CLAUDE.md | **+275** |

### Skipped tests

| Test | Reason |
|------|--------|
| `test_pdf_render.py::test_render_brief_html_wraps_template` | weasyprint not installed — expected, documented |
| `test_pdf_render.py::test_render_pdf_end_to_end` | weasyprint not installed — expected, documented |

### New test files not in CLAUDE.md

`test_brief_schemas.py`, `test_fallback_client.py`, `test_leg_jud.py`, `test_publication_generator.py`, `test_spec1_api_scheduler.py`, `test_tools_generate_brief.py`, `test_tools_generate_leads.py`, `test_tools_run_psyop.py`, `test_ui_route.py`, `test_x_publisher.py`

### Installation blocker

`pip install -e ".[dev]"` fails with `ERROR: Could not build wheels for sgmllib3k`. This is a feedparser transitive dependency (`feedparser>=6.0` → `sgmllib3k`) with a build-system incompatibility in this Python 3.11 environment (distutils `install_layout` attribute error). The test suite runs fine once dependencies are installed via `uv pip install --system`, but the standard `pip install` entrypoint is broken for new contributors.

---

## 5. Hygiene

### Tracked artifacts that should be gitignored

| File / Dir | Size | Status | Note |
|------------|------|--------|------|
| `briefs/` (13 files) | ~50KB total | `.gitignore` says `briefs/` but files were committed before rule was added and never untracked | Generated run outputs — should be untracked |
| `spec1_intelligence_export.json` | 5.7 MB | `.gitignore` has `/spec1_intelligence_export.json` but file is tracked | Largest file in repo; runtime export artifact |
| `spec1_political_web.html` | 39 KB | `.gitignore` has `/spec1_political_web.html` but file is tracked | Generated HTML; duplicated under `spec1_api/static/` |

`spec1_intelligence_export.json` at 5.7 MB is the only file over 1 MB in the repo. It was committed before the gitignore exclusion was added.

### Dependency issues

| Issue | Severity |
|-------|----------|
| `pip install -e ".[dev]"` fails (sgmllib3k build error) | HIGH — breaks onboarding |
| `tweepy` used but not in `pyproject.toml` | MEDIUM — silent missing dep |
| All runtime deps are `>=` unpinned | LOW — acceptable for a service; increases nondeterminism |
| `mcp` package in pyproject.toml — `mcp_server.py` implements JSON-RPC manually and does NOT import `mcp` | LOW — package installed but unused |

### Secrets and sensitive data

No hardcoded API keys, passwords, or tokens found in committed files. `.env` is correctly gitignored. `.env.example` uses placeholder values (`sk-ant-...`).

### Operational noise files

- `REPAIR_PLAN_PR64_66_WORKFLOWS.md` — post-mortem for PR conflicts from PRs #64-66. Lives at repo root; belongs in `memory/` or should be removed once conflicts are resolved.
- `FALLBACK_SETUP.md` — setup instructions for environments without sgmllib3k. Relevant to the install blocker above; keeping it at root is reasonable until the dep issue is fixed.

---

## 6. Recommended Fix Order

Ordered by blast radius (smallest first) and grouped by category.

### Immediate (no code change, documentation only)

1. **Update `memory/context.md`** — set version to `v0.6.0`, remove `cls_quant` from active modules, correct MCP tool count to 15, remove `.[quant]` install reference. One file, < 20 lines.

2. **Update `CLAUDE.md`** — remove 4 `cls_quant` references, remove `test_quant.py` note, remove internal re-export shim references, update test count, update MCP tool count to 15, add `cls_leg_jud` and `spec1_engine/llm` to documented modules. Requires human approval per governance rules since CLAUDE.md itself is in the freeze list.

3. **Move `REPAIR_PLAN_PR64_66_WORKFLOWS.md`** to `memory/` — repo root cleanup.

### Small code fixes (single file, zero functional risk)

4. **Fix duplicate imports in `spec1_api/main.py`** — remove the second `from fastapi import FastAPI` (line 12) and the second `from spec1_api.routers import publication` (line 31). Zero behavior change, tests confirm.

5. **Declare `tweepy` as optional dep in `pyproject.toml`** — add `tweepy>=4.0` to a new `[project.optional-dependencies]` `publish` group, or document its absence prominently in the X publisher module. Currently the publisher silently degrades with no user warning.

### Artifact cleanup (git hygiene, no code change)

6. **Untrack `briefs/` files** — `git rm --cached briefs/*.md briefs/*.jsonl`. The gitignore rule is correct; the files just need to be removed from tracking. The `briefs/` directory was committed deliberately (see `5cb6453`) so confirm with owner before removing.

7. **Untrack `spec1_intelligence_export.json`** — `git rm --cached spec1_intelligence_export.json`. 5.7 MB that was supposed to be excluded. This removes it from HEAD going forward but not from history; if the file contains sensitive intelligence data, consider BFG or git-filter-repo for a history rewrite.

8. **Untrack `spec1_political_web.html`** — `git rm --cached spec1_political_web.html`. Served from `spec1_api/static/` at runtime; the root copy is redundant.

### Dependency / packaging (moderate risk — affects CI)

9. **Fix `sgmllib3k` install path** — Options: pin `feedparser` to a version before sgmllib3k was added as a hard dependency, or add a `setup.cfg` / `pip.conf` workaround. The fastest fix is to pin `feedparser==6.0.10` which does not require sgmllib3k. Needs CI verification across Python 3.9–3.12.

### Architecture decisions (requires human decision, not agent work)

10. **Resolve dual psyop implementations** — `spec1_engine/psyop/scorer.py` (used by cycle) vs `cls_psyop/` (used by MCP/operator tools). Either: (a) have `cycle.py` call `cls_psyop` directly, or (b) document the split as intentional with rationale in `memory/decisions.md`. Currently the two implementations diverge silently.

11. **Document core/prompts modification in `b377099`** — The four geopolitics/legislative prompt files added to `core/prompts/` in `b377099` constitute a governance violation. The prompts are present and working, so no rollback is needed, but the decision should be retroactively recorded in `memory/decisions.md` (e.g., ADR-010: "adding new prompt surface qualifies as MINOR, not a frozen-core modification").

---

*Audit complete. No changes made to the codebase.*
